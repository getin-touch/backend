export enum CONFIG {
  DATABASE = 'database',
  GRAPHQL = 'graphql',
  JWT = 'jwt',
  LOGGER = 'logger',
  NODE_ENV = 'node_env',
}

export enum ENV_TYPES {
  DEV = 'development',
  PROD = 'production',
}
